
  # Thai Cleft(in progress)

  This is a code bundle for Thai Cleft(in progress). The original project is available at https://www.figma.com/design/7c0pAtD0u1EkI0SswLnvyH/Thai-Cleft-in-progress-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Thai-Cleft-Application
# Thai-Cleft-demo
