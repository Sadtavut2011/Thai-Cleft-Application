import React from 'react';
import MobileLayout from './pcu-mobile/layout/MobileLayout';

export default function PCUEntry() {
  // PCU role currently only focuses on Mobile view as per requirements
  return <MobileLayout />;
}
